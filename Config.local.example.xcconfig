// Copy this file to Config.local.xcconfig and fill in client-visible production values.
// Never put service-role, admin, private API or server-only secrets in an iOS client.

REVENUECAT_API_KEY =
// xcconfig treats // as a comment; use https:/$()/example.supabase.co syntax.
SUPABASE_URL =
SUPABASE_PUBLISHABLE_KEY =
GAD_APPLICATION_IDENTIFIER =
GAD_INTERSTITIAL_AD_UNIT_ID =
// Host only, without scheme or path. Example: pinly.example
PINLY_WEBSITE_HOST =
// Numeric App Store ID only.
PINLY_APP_STORE_ID =
